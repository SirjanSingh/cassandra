# Archived docs

These are hackathon-era planning, pitch, and market docs kept for history. They are
**not** the current source of truth — see the active `docs/` for that. Nothing here is
referenced by the code.

Current, maintained docs live one level up in `docs/` (architecture, workflows, testing,
production adoption, the NLI/multilingual design), plus `README.md` at the repo root.
